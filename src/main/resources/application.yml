spring:
  application:
    name: 2nd_SchoolStock

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://127.0.0.1:3306/mydb
    username: mydbid
    password: mydbpw

server:
    port: 8882

mybatis:
  mapper-locations: classpath:mappers/*.xml