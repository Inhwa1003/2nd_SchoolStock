<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.school.schoolstock.domain.coupon_purchase.repository.CouponPurchaseRepository">

    <!-- 쿠폰 구매 내역 등록 -->
    <insert id="setPurchaseRecord">
        INSERT INTO COUPON_PURCHASE ( student_id, coupon_no, purchase_date, purchase_point, name, purchase_state )
        VALUES ( #{studentId}, #{couponNo}, NOW(), #{couponPrice}, #{couponName}, #{purchaseState} )
    </insert>

</mapper>